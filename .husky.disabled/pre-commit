#!/usr/bin/env sh
# Husky v10+ compatible: avoid deprecated _/husky.sh bootstrap
set -e

# Run from repo root so tools resolve paths consistently
cd -- "$(dirname "$0")/.." || exit 1

npx lint-staged
